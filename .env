# SQL connection. Default: zero-config local SQLite file.
# To use PostgreSQL instead, set e.g.:
#   DATABASE_URL="postgresql://user:pass@localhost:5432/comiclettering"
# and change `provider` to "postgresql" in prisma/schema.prisma.
DATABASE_URL="file:./dev.db"
